/**
 * 1. var --> function scoped + changeable
 * 2. let --> block scoped + changeable
 * 3. const --> block scoped + nonchangeable
 * 
 * 4. template string --> ` ${}`
 * 
 * 5. arraw function: () => {return }
 * or () => ...;
 * 
 * 6. destructuring: arr = [1, 2];
 *                  [x, y] = arr;
 *      => [..] = array
 *      => {..} = obj; 
 * 
 * 7. spread operation: ...
 * 8. Object.keys, Object.values, Object.entries
 * 
 * 9. for-of: array and string
 * 10. for-in: object(keys)
 */